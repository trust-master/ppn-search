!!!
%html
  %head
    %title= ['Trust Master', page_title].compact.join(': ')

    = stylesheet_link_tag :application
    /[if IE 8]
      = stylesheet_link_tag :ie8
    /[if lte IE 7]
      = stylesheet_link_tag :ie7
    = javascript_include_tag :application

    = content_for :head

  %body
    #wrapper
      %header#header
        %h1
          = link_to root_url do
            = image_tag asset_path('logo.png'), alt: "Trust Master: #{t(:tagline)}"
        - if current_user
          %nav.header-nav
            %span.welcome= t(:welcome, name: current_user.first_name)
            = nav_for :account_links, menu_class: 'account-links', item_wrapper: nil do
              %li.nav-profile
                .divider
                = nav_link :profile, edit_user_path
                = nav_for :profile do
                  = nav_link :show, user_path
                  = nav_link :edit, edit_user_path
              %li.nav-account
                .divider
                = nav_link :account, '/account'
                = nav_for :account do
                  = nav_link :basic_info, '/account/basic_info'
                  = nav_link :privacy_settings, '/account/privacy'
                  = nav_link :notifications_alerts, '/account/notifications_alerts'
              %li.nav-logout
                = nav_link :logout, logout_path
            .clear
          .search
            = form_tag({controller: :companies, action: :index}, method: :get)  do
              %fieldset
                %input{type: :search, name: :search}
                %input{type: :submit}

        %nav.main-nav
          = nav_for :main do
            - if current_user
              = nav_link :search_ppn, companies_path, proc { request.path.include? '/companies' }
              = nav_link :admin, '/admin'
            - else
              = nav_link :login, login_path

      #content{class: [content_for(:content_div_class).presence || 'col-2', page_class] }

        - if show_title?
          %h2= page_title

        - flash.each do |bucket, messages|
          - Array.wrap(messages).each do |msg|
            .flash{ class: bucket }= msg

        = content_for?(:content) ? yield(:content) : yield

        .clear

      %footer#footer
        .footer-links
          = nav_for :footer do
            - [:terms, :privacy_policy, :about, :contact].each do |page_id|
              = nav_link page_id, page_id.to_s.prepend('/')

        .social-media
          %h5= t(:follow_us_on)
          %ul
            %li
              = link_to '' do
                = image_tag asset_path('twitter.png'), alt: 'Twitter'
            %li
              = link_to '' do
                = image_tag asset_path('facebook.png'), alt: 'Facebook'

        .trust-logos
          = image_tag asset_path('trust-logos.png'), alt: 'Verisign'

    #copyright &copy; #{Time.now.year} #{t('copyright')}

  -#
    #development
      %h2 Development Stuff
      = render 'disqus'
      - unless Rails.env.production?
        = whereuat # the helper to load the Whereuat plugin to aid PivotalTracker story management
