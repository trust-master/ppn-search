%a{:href => "/companies/"} Return to Index
%h2
  Edit #{@company.name}
.notifications.hidden
%fieldset.edit_company
  %input#form_destination{:type => "hidden", :value => "/companies/#{@company.id}"}/
  = render :partial => 'form', :locals => { :company => @company }
