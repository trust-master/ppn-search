- title t('.title', company_name: @company.name)

= simple_form_for @company, multipart: true do |form|

  = render 'credentials_fieldsets', form: form

  %fieldset.last#buttons
    = link_to t('helpers.cancel'), @company, class: :cancel
    = form.button :submit, class: :save