%p
  - if show.current_member == true
    Currently affiliated with #{show.name}
  - else
    Affiliated with #{show.name} from #{show.start_month} / #{show.start_year}
    until #{show.end_month} / #{show.end_year}.
%span
  %a.delete{:href => "/affiliations/#{show.id}/delete"} Delete
