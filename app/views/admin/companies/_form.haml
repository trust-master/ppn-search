= simple_form_for [:admin, @company], multipart: true do |form|

  - %w[listing capabilities credentials].each do |section|
    = render "#{section}_fieldsets", form: form

  %fieldset.last#buttons
    = link_to t('helpers.cancel'), @company, class: :cancel
    = form.button :submit, class: :save