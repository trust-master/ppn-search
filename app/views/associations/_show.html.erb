<p>
  <% if show.current_member == true %>
    Currently associated with <%= show.name %>
  <% else %>
    Associated with <%= show.name %> from <%= show.start_month %> / <%= show.start_year %>
    until <%= show.end_month %> / <%= show.end_year %>.
  <% end %>
</p>
<span><a href="/associations/<%=show.id%>/delete" class="delete">Delete</a></span>
