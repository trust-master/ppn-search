= builder.hidden_field :_destroy, {class: :destroy}
= builder.input :city
= builder.input :zip
-#= builder.association :country, priority: ['United States']
= builder.input :state do
  = builder.grouped_collection_select :state_id, Country.includes(:states), :states, :name, :id, :name
